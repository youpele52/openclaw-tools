# Commodities tests

This folder contains a simple smoke test for the commodities skill. Run:

```bash
bash tests/test_commodities_smoke.sh
```

The test is a lightweight smoke test and may fail when offline or when Yahoo endpoints are unreachable.
